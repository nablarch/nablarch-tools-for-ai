# Nablarchバッチアプリケーション基本構造

## 基本実装パターン

```java
// 基本的なバッチアクション
public class SampleBatchAction extends BatchAction<SqlRow> {
    @Override
    public DataReader<SqlRow> createReader(ExecutionContext context) {
        return new DatabaseRecordReader();
    }
    
    @Override
    public Result handle(SqlRow inputData, ExecutionContext context) {
        // 1レコードの処理
        return new Success();
    }
    
    @Override
    protected void initialize(CommandLine command, ExecutionContext context) {
        // 初期化処理（任意）
    }
    
    @Override
    protected void terminate(Result result, ExecutionContext context) {
        // 終了処理（任意）
    }
}

// データリーダ実装
public class DatabaseRecordReader implements DataReader<SqlRow> {
    @Override
    public SqlRow read(ExecutionContext ctx) {
        // 1レコード読み込み
    }
    
    @Override
    public boolean hasNext(ExecutionContext ctx) {
        // 次レコードの有無
    }
    
    @Override
    public void close(ExecutionContext ctx) {
        // リソース解放
    }
}

// フォームクラス
public class BatchForm {
    @Domain("projectId")
    @Required
    private String projectId;
    
    @Domain("projectName")
    private String projectName;
    
    // getter/setter省略
}
```

## 必須実装メソッド

### BatchAction継承
- `createReader(ExecutionContext context)`: DataReader生成
- `handle(TData inputData, ExecutionContext context)`: 1レコード処理

### DataReader実装  
- `read(ExecutionContext ctx)`: データ読み込み
- `hasNext(ExecutionContext ctx)`: 次データ判定
- `close(ExecutionContext ctx)`: リソース解放

## 設計原則 - フレームワーク制御

```java
// ❌ 自前ループ
public class WrongAction {
    public Result handle(ExecutionContext context) {
        List<Data> dataList = getDataList();  // 全件取得リスク
        for (Data data : dataList) {
            // 処理
        }
    }
}

// ✅ フレームワーク制御
public class CorrectAction extends BatchAction<SqlRow> {
    @Override
    public DataReader<SqlRow> createReader(ExecutionContext context) {
        return new DatabaseRecordReader();  // フレームワークがループ制御
    }
    
    @Override
    public Result handle(SqlRow inputData, ExecutionContext context) {
        // 1レコード処理
        return new Success();
    }
}
```

## データリーダ実装例

```java
// DB読み込み
@Override
public DataReader<SqlRow> createReader(ExecutionContext context) {
    DatabaseRecordReader reader = new DatabaseRecordReader();
    SqlPStatement statement = getSqlPStatement("FIND_DATA");
    reader.setStatement(statement);
    return reader;
}

// ファイル読み込み
public class FileDataReader implements DataReader<FormClass> {
    private ObjectMapperIterator<FormClass> iterator;
    
    @Override
    public FormClass read(ExecutionContext ctx) {
        if (iterator == null) {
            FilePathSetting setting = FilePathSetting.getInstance();
            File file = setting.getFileWithoutCreate("input", "fileName");
            iterator = new ObjectMapperIterator<>(
                ObjectMapperFactory.create(FormClass.class, new FileInputStream(file))
            );
        }
        return iterator.next();
    }
    
    @Override
    public boolean hasNext(ExecutionContext ctx) {
        return iterator.hasNext();
    }
    
    @Override
    public void close(ExecutionContext ctx) {
        iterator.close();
    }
}
```

## ファイル出力パターン

```java
public class FileOutputAction extends BatchAction<SqlRow> {
    private ObjectMapper<OutputDto> mapper;
    
    @Override
    protected void initialize(CommandLine command, ExecutionContext context) {
        FilePathSetting setting = FilePathSetting.getInstance();
        File file = setting.getFile("output", "fileName");
        this.mapper = ObjectMapperFactory.create(OutputDto.class, new FileOutputStream(file));
    }
    
    @Override
    public Result handle(SqlRow inputData, ExecutionContext context) {
        OutputDto dto = EntityUtil.createEntity(OutputDto.class, inputData);
        mapper.write(dto);
        return new Success();
    }
    
    @Override
    protected void terminate(Result result, ExecutionContext context) {
        mapper.close();
    }
}
```

## 基本ユーティリティ

```java
// 業務日付
String businessDate = BusinessDateUtil.getDate();

// データ変換
OutputDto dto = EntityUtil.createEntity(OutputDto.class, sqlRow);
EntityClass entity = BeanUtil.createAndCopy(EntityClass.class, form);

// DB操作
UniversalDao.findBySqlFile(EntityClass.class, "FIND_SQL", params);
UniversalDao.insert(entity);
```

