# Nablarchバッチ開発チェックリスト（生成AI用）

## 必須実装チェック

### BatchAction継承
```java
// ✅ 必須
public class XxxAction extends BatchAction<InputType> {
    @Override
    public DataReader<InputType> createReader(ExecutionContext context) { /* 必須 */ }
    
    @Override  
    public Result handle(InputType inputData, ExecutionContext context) { /* 必須 */ }
    
    @Override
    protected void initialize(CommandLine command, ExecutionContext context) { /* 任意 */ }
    
    @Override
    protected void terminate(Result result, ExecutionContext context) { /* 任意 */ }
}
```

### DataReader実装
```java
// ✅ 必須メソッド
public class XxxDataReader implements DataReader<FormType> {
    @Override
    public FormType read(ExecutionContext ctx) { /* 必須 */ }
    
    @Override
    public boolean hasNext(ExecutionContext ctx) { /* 必須 */ }
    
    @Override
    public void close(ExecutionContext ctx) { /* 必須 */ }
}
```

### ファイル出力時
```java
// ✅ initialize/terminateパターン
private ObjectMapper<OutputDto> mapper;

@Override
protected void initialize(CommandLine command, ExecutionContext context) {
    FilePathSetting setting = FilePathSetting.getInstance();
    File file = setting.getFile("output", "fileName");
    mapper = ObjectMapperFactory.create(OutputDto.class, new FileOutputStream(file));
}

@Override
protected void terminate(Result result, ExecutionContext context) {
    mapper.close();  // リソース解放必須
}
```

## 基本ユーティリティ使用チェック

```java
// ✅ 業務日付
String date = BusinessDateUtil.getDate();

// ✅ データ変換
OutputDto dto = EntityUtil.createEntity(OutputDto.class, sqlRow);
Entity entity = BeanUtil.createAndCopy(Entity.class, form);

// ✅ ファイルパス管理
FilePathSetting setting = FilePathSetting.getInstance();
File file = setting.getFile("output", "fileName");

// ✅ DB操作
UniversalDao.insert(entity);
List<Entity> list = UniversalDao.findBySqlFile(Entity.class, "SQL_ID", params);
```

## 禁止事項チェック

```java
// ❌ 自前ループ
List<Data> list = dao.findAll();
for (Data data : list) { /* 処理 */ }

// ❌ 毎回ファイル開閉
try (FileWriter writer = new FileWriter("file.csv", true)) {
    writer.write(line);
}

// ❌ 手動CSV生成
String csv = data.getId() + "," + data.getName();

// ❌ システム日付使用
String date = new SimpleDateFormat("yyyyMMdd").format(new Date());

// ❌ フレームワーク継承なし
public class PlainClass {
    public void process() { /* 処理 */ }
}
```

## フォーマット設定チェック

```java
// ✅ CSV
@Csv(properties = {"id", "name"}, type = CsvType.CUSTOM)
@CsvFormat(charset = "UTF-8", fieldSeparator = ',')
public class CsvForm {
    private String id;
    private String name;
}

// ✅ 固定長  
@FixedLength(length = 30, charset = "UTF-8")
public class FixedForm {
    @Field(offset = 1, length = 10)
    private String id;
    
    @Field(offset = 11, length = 20)
    private String name;
}
```

## パフォーマンスチェック

- フレームワーク制御（自前ループ禁止）
- バッチ処理使用（`UniversalDao.batchInsert()`）
- N+1問題回避（JOINクエリ使用）
- 遅延ロード考慮（大量データ処理時）
- 適切なリソース解放
- [ ] try-with-resourcesの不適切な使用を回避

### 13. トランザクション制御
- [ ] フレームワークのトランザクション制御に委譲
- [ ] 手動でのトランザクション制御を回避
- [ ] 適切な`Result`を返却（Success/Error）

### 14. ログ出力
- [ ] 処理開始・終了ログが出力されている
- [ ] 進捗ログが適切な間隔で出力されている
- [ ] エラーログに十分な情報が含まれている
- [ ] デバッグログが適切に配置されている

## アンチパターンチェック項目

### 15. 禁止事項チェック
- [ ] ❌ 自前でのループ処理を実装していない
- [ ] ❌ 全件データの一括取得を行っていない
- [ ] ❌ 毎回のファイル開閉を行っていない
- [ ] ❌ 手動でのCSV文字列生成を行っていない
- [ ] ❌ システム日付を業務日付として使用していない
- [ ] ❌ フレームワーク継承なしのクラスを使用していない
- [ ] ❌ 手動でのトランザクション制御を行っていない
- [ ] ❌ 1件エラーで全体処理を停止していない

## 設定チェック項目

### 16. ファイルパス管理
- [ ] `FilePathSetting`の設定が正しく行われている
- [ ] 論理名とディレクトリのマッピングが適切
- [ ] 拡張子の設定が正しい
- [ ] ファイルパスにハードコーディングがない

### 17. ハンドラ構成
- [ ] 必要最小限のハンドラが設定されている
- [ ] ハンドラの順序が正しい
- [ ] DB接続の有無に応じた構成になっている
- [ ] エラーハンドラが適切に設定されている

## テスト項目

### 18. 単体テスト
- [ ] 正常パターンのテストが実装されている
- [ ] 異常パターンのテストが実装されている
- [ ] 境界値のテストが実装されている
- [ ] バリデーションエラーのテストが実装されている

### 19. 結合テスト
- [ ] 実際のファイル/DBでの動作確認
- [ ] 大量データでの性能確認
- [ ] エラー時の動作確認
- [ ] ログ出力の確認

## 実装完了後チェック項目

### 20. コードレビュー
- [ ] Nablarchの設計思想に沿った実装になっている
- [ ] パフォーマンスを考慮した実装になっている
- [ ] 保守性を考慮したコードになっている
- [ ] 例外的なケースが考慮されている

### 21. ドキュメント
- [ ] 処理概要が記載されている
- [ ] パラメータ仕様が記載されている
- [ ] エラー処理方針が記載されている
- [ ] 運用手順が記載されている

### 22. 運用準備
- [ ] 実行コマンドが準備されている
- [ ] 監視項目が定義されている
- [ ] エラー時の対処手順が準備されている
- [ ] バックアップ・リカバリ手順が準備されている

## 重要な確認ポイント

### 🔍 特に重要な確認項目
1. **フレームワーク制御**: 自前ループではなく`createReader()`/`handle()`パターン
2. **リソース管理**: `initialize()`/`terminate()`での適切な管理
3. **業務日付**: `BusinessDateUtil.getDate()`の使用
4. **データ変換**: `EntityUtil`/`BeanUtil`の活用
5. **エラーハンドリング**: 1件エラーで全体停止しない設計

このチェックリストを活用して、Nablarchフレームワークに適合した高品質なバッチアプリケーションを開発してください。
