# 3.6 DB出力パターン

```java
@ValidateData
public Result handle(ImportForm inputData, ExecutionContext context) {
    try {
        validateBusinessRules(inputData);
        
        // 変換: SqlRow→Entity: EntityUtil.createEntity()（1.8_object_conversion.md参照）、Form→Entity: BeanUtil.createAndCopy()（1.3_bean_util.md参照）
        DataEntity entity = BeanUtil.createAndCopy(DataEntity.class, inputData);
        entity.setCreatedDate(BusinessDateUtil.getDate());
        
        UniversalDao.insert(entity);
        return new Success();
        
    } catch (ApplicationException e) {
        LOGGER.warn("エラー 行:{}", inputData.getLineNumber(), e);
        throw e;
    }
}
```

## CRUD操作

### 新規登録
```java
DataEntity entity = EntityUtil.createEntity(DataEntity.class, inputData);
entity.setCreatedDate(BusinessDateUtil.getDate());
UniversalDao.insert(entity);
```

### 更新
```java
DataEntity existing = UniversalDao.findById(DataEntity.class, inputData.getId());
BeanUtil.copy(inputData, existing);
existing.setUpdatedDate(BusinessDateUtil.getDate());
UniversalDao.update(existing);
```

### Upsert
```java
if (UniversalDao.exists(DataEntity.class, "FIND_BY_ID", inputData.getId())) {
    // 更新処理
    DataEntity existing = UniversalDao.findBySqlFile(DataEntity.class, "FIND_BY_ID", inputData.getId());
    BeanUtil.copy(inputData, existing);
    existing.setUpdatedDate(BusinessDateUtil.getDate());
    UniversalDao.update(existing);
} else {
    // 新規登録
    DataEntity entity = BeanUtil.createAndCopy(DataEntity.class, inputData);
    entity.setCreatedDate(BusinessDateUtil.getDate());
    UniversalDao.insert(entity);
}
```

## 業務ルールチェック

```java
private void validateBusinessRules(ImportForm form) {
    List<Message> messages = new ArrayList<>();
    
    // 重複チェック
    if (UniversalDao.exists(DataEntity.class, "FIND_BY_ID", form.getId())) {
        messages.add(MessageUtil.createMessage(MessageLevel.ERROR, "duplicate.id", form.getId()));
    }
    
    // 参照整合性チェック
    if (!UniversalDao.exists(MasterEntity.class, "FIND_BY_CODE", form.getMasterCode())) {
        messages.add(MessageUtil.createMessage(MessageLevel.ERROR, "not.found.master", form.getMasterCode()));
    }
    
    if (!messages.isEmpty()) {
        throw new ApplicationException(messages);
    }
}
```